import { createSlice } from '@reduxjs/toolkit';

const blueprintStory = createSlice({
  name: 'blueprintStory',
  initialState: {
    listStory: [],
    currentStory: []
  },
  reducers: {
    // kika på tidigare projekt labyrinth
  }
});

export default blueprintStory;
