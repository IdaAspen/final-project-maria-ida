import { createSlice } from '@reduxjs/toolkit';

const characters = createSlice({
  name: 'storyblabla',
  initialState: {
    characters: [],
    personality: [],
    elements: []
  },
  reducers: {}
});

export default characters;
